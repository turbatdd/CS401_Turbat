package lab10;

public class Main {

    /*
    1.A.1
    Runnable r = () →
    {
        int[][] products = new int[s][t];
        for (int i = 0; i < s; i++) {
            for(int j = i + 1; j < t; j++) {
                products[i][j] = i * j;
            }
        }
    }

    Answer:
    No parameters
    S and T are free variables

    1.A.2
    Comparator<String> comp = (s, t) →
    {
        if(ignoreCase == true) {
        return s.compareToIgnoreCase(t);
        } else {
        return s.compareTo(t);
        }
    }

    Answer:
    S and T are parameters
    ignoreCase is free variable
     */
}
